import{_ as p,r as l,o as u,c as h,a as e,b as n,t as c,d as a,w as i,n as m,g as v,e as o}from"./app-d223f2a5.js";const b={},g={id:"frontmatter-title-관련",tabindex:"-1"},f=e("a",{class:"header-anchor",href:"#frontmatter-title-관련","aria-hidden":"true"},"#",-1),k={class:"table-of-contents"},x=e("hr",null,null,-1),w=o(`<hr><h2 id="_1-stdout-standard-out" tabindex="-1"><a class="header-anchor" href="#_1-stdout-standard-out" aria-hidden="true">#</a> 1. <code>stdout</code> (Standard Out)</h2><p>By now, we&#39;ve become familiar with many commands and their output and that brings us to our next subject I/O (input/output) streams. Let&#39;s run the following command and we&#39;ll discuss how this works.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> Hello World <span class="token operator">&gt;</span> peanuts.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>What just happened? Well check the directory where you ran that command and lo and behold you should see a file called peanuts.txt, look inside that file and you should see the text Hello World. Lots of things just happened in one command so let&#39;s break it down.</p><p>First let&#39;s break down the first part:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> Hello World
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>We know this prints out Hello World to the screen, but how? Processes use I/O streams to receive input and return output. By default the echo command takes the input (standard input or stdin) from the keyboard and returns the output (standard output or <code>stdout</code>) to the screen. So that&#39;s why when you type echo Hello World in your shell, you get Hello World on the screen. However, I/O redirection allows us to change this default behavior giving us greater file flexibility.</p><p>Let&#39;s proceed to the next part of the command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code> <span class="token operator">&gt;</span> 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The &gt; is a redirection operator that allows us the change where standard output goes. It allows us to send the output of echo Hello World to a file instead of the screen. If the file does not already exist it will create it for us. However, if it does exist it will overwrite it (you can add a shell flag to prevent this depending on what shell you are using).</p><p>And that&#39;s basically how <code>stdout</code> redirection works!</p>`,12),y=e("code",null,"peanuts.txt",-1),_=e("code",null,">>",-1),T=o(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> Hello World <span class="token operator">&gt;&gt;</span> peanuts.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,1),q=e("code",null,"peanuts.txt",-1),W=e("code",null,">",-1),j=o(`<p>Try a couple of commands:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> <span class="token parameter variable">-l</span> /var/log <span class="token operator">&gt;</span> myoutput.txt
<span class="token builtin class-name">echo</span> Hello World <span class="token operator">&gt;</span> <span class="token function">rm</span>
<span class="token operator">&gt;</span> somefile.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><hr><h2 id="_2-stdin-standard-in" tabindex="-1"><a class="header-anchor" href="#_2-stdin-standard-in" aria-hidden="true">#</a> 2. <code>stdin</code> (Standard In)</h2><p>In our previous lesson we learned that we have different stdout streams we can use, such as a file or the screen. Well there are also different standard input (<code>stdin</code>) streams we can use as well. We know that we have <code>stdin</code> from devices like the keyboard, but we can use files, output from other processes and the terminal as well, let&#39;s see an example.</p><p>Let&#39;s use the peanuts.txt file in the previous lesson for this example, remember it had the text Hello World in it.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> <span class="token operator">&lt;</span> peanuts.txt <span class="token operator">&gt;</span> banana.txt 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Just like we had <code>&gt;</code> for <code>stdout</code> redirection, we can use <code>&lt;</code> for <code>stdin</code> redirection.</p>`,8),I=e("code",null,"stdin",-1),A=e("code",null,"peanuts.txt",-1),H=e("code",null,"peanuts.txt",-1),L=e("code",null,"banana.txt",-1),S=o(`<p>Try out a couple of commands:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> <span class="token operator">&lt;</span> peanuts.txt <span class="token operator">&gt;</span> banana.txt
<span class="token function">ls</span> <span class="token operator">&lt;</span> peanuts.txt <span class="token operator">&gt;</span> banana.txt
<span class="token builtin class-name">pwd</span> <span class="token operator">&lt;</span> peanuts.txt <span class="token operator">&gt;</span> banana.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><hr><h2 id="_3-stderr-standard-error" tabindex="-1"><a class="header-anchor" href="#_3-stderr-standard-error" aria-hidden="true">#</a> 3. stderr (Standard Error)</h2><p>Let&#39;s try something a little different now, let&#39;s try to list the contents of a directory that doesn&#39;t exist on your system and redirect the output to the peanuts.txt file again.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> /fake/directory <span class="token operator">&gt;</span> peanuts.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>What you should see is:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>ls: cannot access /fake/directory: No such file or directory
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now you&#39;re probably thinking, shouldn&#39;t that message have been sent to the file? There is actually another I/O stream in play here called standard error (<code>stderr</code>). By default, <code>stderr</code> sends its output to the screen as well, it&#39;s a completely different stream than <code>stdout</code>. So you&#39;ll need to redirect its output a different way.</p><p>Unfortunately the redirector is not as nice as using <code>&lt;</code> or <code>&gt;</code> but it&#39;s pretty close. We will have to use file descriptors. A file descriptor is a non-negative number that is used to access a file or stream. We will go in depth about this later, but for now know that the file descriptor for <code>stdin</code>, <code>stdout</code> and <code>stderr</code> is 0, 1, and 2 respectively.</p><p>So now if we want to redirect our stderr to the file we can do this:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> /fake/directory <span class="token operator"><span class="token file-descriptor important">2</span>&gt;</span> peanuts.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,12),B=e("code",null,"peanuts.txt",-1),Y=e("code",null,"peanuts.txt",-1),P=o(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> /fake/directory <span class="token operator">&gt;</span> peanuts.txt <span class="token operator"><span class="token file-descriptor important">2</span>&gt;</span><span class="token file-descriptor important">&amp;1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,1),z=e("code",null,"ls",-1),N=e("code",null,"/fake/directory",-1),$=e("code",null,"peanuts.txt",-1),E=e("code",null,"stderr",-1),O=e("code",null,"2>&1",-1),D=e("code",null,"2>&1",-1),J=e("code",null,"2>&1",-1),F=e("code",null,"peanuts.txt",-1),U=e("code",null,"stderr",-1),V=e("code",null,"stdout",-1),C=o(`<p>There is a shorter way to redirect both <code>stdout</code> and <code>stderr</code> to a file:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> /fake/directory <span class="token operator">&amp;&gt;</span> peanuts.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,2),M=e("code",null,"/dev/null",-1),R=o(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> /fake/directory <span class="token operator"><span class="token file-descriptor important">2</span>&gt;</span> /dev/null
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>What is the following command doing?</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> /fake/directory <span class="token operator">&gt;&gt;</span> /dev/null <span class="token operator"><span class="token file-descriptor important">2</span>&gt;</span><span class="token file-descriptor important">&amp;1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><hr><h2 id="_4-pipe-and-tee" tabindex="-1"><a class="header-anchor" href="#_4-pipe-and-tee" aria-hidden="true">#</a> 4. pipe and tee</h2><p>Let&#39;s get into some plumbing now, not really but kinda. Let&#39;s try a command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> <span class="token parameter variable">-la</span> /etc
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You should see a very long list of items, it&#39;s a little hard to read actually. Instead of redirecting this output to a file, wouldn&#39;t it be nice if we could just see the output in another command like less? Well we can!</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> <span class="token parameter variable">-la</span> /etc <span class="token operator">|</span> <span class="token function">less</span> 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The pipe operator <code>|</code>, represented by a vertical bar, allows us to get the <code>stdout</code> of a command and make that the <code>stdin</code> to another process. In this case, we took the stdout of <code>ls -la /etc</code> and then piped it to the <code>less</code> command. The pipe command is extremely useful and we will continue to use it for all eternity.</p><p>Well what if I wanted to write the output of my command to two different streams? That&#39;s possible with the tee command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> <span class="token operator">|</span> <span class="token function">tee</span> peanuts.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,12),Z=e("code",null,"peanuts.txt",-1),G=o(`<p>Try the following commands:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> <span class="token operator">|</span> <span class="token function">tee</span> peanuts.txt banan.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><hr><h2 id="_5-env-environment" tabindex="-1"><a class="header-anchor" href="#_5-env-environment" aria-hidden="true">#</a> 5. env (Environment)</h2><p>Run the following command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> <span class="token environment constant">$HOME</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You should see the path to your home directory, mine looks like /home/pete.</p><p>What about this command?</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> <span class="token environment constant">$USER</span> 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You should see your username!</p><p>Where is this information coming from? It&#39;s coming from your environment variables. You can view these by typing</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">env</span> 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This outputs a whole lot of information about the environment variables you currently have set. These variables contain useful information that the shell and other processes can use.</p><p>Here is a short example:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token assign-left variable"><span class="token environment constant">PATH</span></span><span class="token operator">=</span>/usr/local/sbin:/usr/local/bin:/usr/sbin:/bin
<span class="token assign-left variable"><span class="token environment constant">PWD</span></span><span class="token operator">=</span>/home/user
<span class="token assign-left variable"><span class="token environment constant">USER</span></span><span class="token operator">=</span>pete
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>One particularly important variable is the PATH Variable. You can access these variables by sticking a <code>$</code> infront of the variable name like so:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token builtin class-name">echo</span> <span class="token environment constant">$PATH</span>
<span class="token comment"># /usr/local/sbin:/usr/local/bin:/usr/sbin:/bin</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>This returns a list of paths separated by a colon that your system searches when it runs a command. Let&#39;s say you manually download and install a package from the internet and put it in to a non standard directory and want to run that command, you type <code>$</code> coolcommand and the prompt says command not found. Well that&#39;s silly you are looking at the binary in a folder and know it exists. What is happening is that $PATH variable doesn&#39;t check that directory for this binary so it&#39;s throwing an error.</p><p>Let&#39;s say you had tons of binaries you wanted to run out of that directory, you can just modify the <code>PATH</code> variable to include that directory in your <code>PATH</code> environment variable.</p><p>What does the following output? Why?</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> <span class="token environment constant">$HOME</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><hr><h2 id="_6-cut" tabindex="-1"><a class="header-anchor" href="#_6-cut" aria-hidden="true">#</a> 6. <code>cut</code></h2><p>We&#39;re gonna learn a couple of useful commands that you can use to process text. Before we get started, let&#39;s create a file that we&#39;ll be working with. Copy and paste the following command, once you do that add a <kbd>TAB</kbd> in between lazy and dog (hold down <kbd>Ctrl</kbd>-<kbd>v</kbd> + <kbd>TAB</kbd>).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> <span class="token string">&#39;The quick brown; fox jumps over the lazy  dog&#39;</span> <span class="token operator">&gt;</span> sample.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>First command we&#39;ll be learning about is the <code>cut</code> command. It extracts portions of text from a file.</p><p>To extract contents by a list of characters:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cut</span> <span class="token parameter variable">-c</span> <span class="token number">5</span> sample.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This outputs the 5th character in each line of the file. In this case it is &quot;q&quot;, note that the space also counts as a character.</p><p>To extract the contents by a field, we&#39;ll need to do a little modification:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cut</span> <span class="token parameter variable">-f</span> <span class="token number">2</span> sample.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The <code>-f</code> or field flag cuts text based off of fields, by default it uses TABs as delimiters, so everything separated by a <kbd>TAB</kbd> is considered a field. You should see &quot;dog&quot; as your output.</p><p>You can combine the field flag with the delimiter flag to extract the contents by a custom delimiter:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cut</span> <span class="token parameter variable">-f</span> <span class="token number">1</span> <span class="token parameter variable">-d</span> <span class="token string">&quot;;&quot;</span> sample.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This will change the <kbd>TAB</kbd> delimiter to a &quot;;&quot; delimiter and since we are cutting the first field, the result should be &quot;The quick brown&quot;.</p><p>What does the following command do? Why?</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cut</span> <span class="token parameter variable">-c</span> <span class="token number">5</span>-10 sample.txt
<span class="token function">cut</span> <span class="token parameter variable">-c</span> <span class="token number">5</span>- sample.txt
<span class="token function">cut</span> <span class="token parameter variable">-c</span> <span class="token parameter variable">-5</span> sample.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><hr><h2 id="_7-paste" tabindex="-1"><a class="header-anchor" href="#_7-paste" aria-hidden="true">#</a> 7. <code>paste</code></h2><p>The paste command is similar to the <code>cat</code> command, it merges lines together in a file. Let&#39;s create a new file with the following contents:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>sample2.txt
The
quick
brown
fox
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Let&#39;s combine all these lines into one line:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">paste</span> <span class="token parameter variable">-s</span> sample2.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The default delimiter for paste is <kbd>TAB</kbd>, so now there is one line with TABs separating each word.</p><p>Let&#39;s change this delimiter (<code>-d</code>) to something a little more readable:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">paste</span> <span class="token parameter variable">-d</span> <span class="token string">&#39; &#39;</span> <span class="token parameter variable">-s</span> sample2.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now everything should be on one line delimited by spaces.</p><hr><h2 id="_8-head" tabindex="-1"><a class="header-anchor" href="#_8-head" aria-hidden="true">#</a> 8. <code>head</code></h2><p>Let&#39;s say we have a very long file, in fact we have many to choose from, go ahead and <code>cat /var/log/syslog</code>. You should see pages upon pages of text. What if I just wanted to see the first couple of lines in this text file? Well we can do that with the head command, by default the head command will show you the first 10 lines in a file.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">head</span> /var/log/syslog
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can also modify the line count to whatever you choose, let&#39;s say I wanted to see the first 15 lines instead.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">head</span> <span class="token parameter variable">-n</span> <span class="token number">15</span> /var/log/syslog
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The <code>-n</code> flag stands for number of lines.</p><p>What does the following command do and why?</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">head</span> <span class="token parameter variable">-c</span> <span class="token number">15</span> /var/log/syslog
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><hr><h2 id="_9-tail" tabindex="-1"><a class="header-anchor" href="#_9-tail" aria-hidden="true">#</a> 9. <code>tail</code></h2><p>Similar to the head command, the tail command lets you see the last 10 lines of a file by default.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tail</span> /var/log/syslog
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Along with head you can change the number of lines you want to see.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tail</span> <span class="token parameter variable">-n</span> <span class="token number">10</span> /var/log/syslog
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Another great option you can use is the <code>-f</code> (follow) flag, this will follow the file as it grows. Give it a try and see what happens.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tail</span> <span class="token parameter variable">-f</span> /var/log/syslog
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Your syslog file will be continually changing while you interact with your system and using tail <code>-f</code> you can see everything that is getting added to that file.</p><p>Look at the man page of tail and read some of the other commands we didn&#39;t discuss.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">man</span> <span class="token function">tail</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><hr><h2 id="_10-expand-and-unexpand" tabindex="-1"><a class="header-anchor" href="#_10-expand-and-unexpand" aria-hidden="true">#</a> 10. <code>expand</code> and <code>unexpand</code></h2>`,69),K=e("code",null,"sample.txt",-1),Q=o(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">expand</span> sample.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The command above will print output with each TAB converted into a group of spaces. To save this output in a file, use output redirection like below.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">expand</span> sample.txt <span class="token operator">&gt;</span> result.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Opposite to expand, we can convert back each group of spaces to a TAB with the unexpand command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">unexpand</span> <span class="token parameter variable">-a</span> result.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><hr><h2 id="_11-join-and-split" tabindex="-1"><a class="header-anchor" href="#_11-join-and-split" aria-hidden="true">#</a> 11. <code>join</code> and <code>split</code></h2><p>The join command allows you to join multiple files together by a common field:</p><p>Let&#39;s say I had two files that I wanted to join together:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> file1.txt
<span class="token comment"># 1 John</span>
<span class="token comment"># 2 Jane</span>
<span class="token comment"># 3 Mary</span>

<span class="token function">cat</span> file2.txt
<span class="token comment"># 1 Doe</span>
<span class="token comment"># 2 Doe</span>
<span class="token comment"># 3 Sue</span>

<span class="token function">join</span> file1.txt file2.txt
<span class="token comment"># 1 John Doe</span>
<span class="token comment"># 2 Jane Doe</span>
<span class="token comment"># 3 Mary Sue</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>See how it joined together my files? They are joined together by the first field by default and the fields have to be identical, if they are not you can sort them, so in this case the files are joined via 1, 2, 3.</p><p>How would we join the following files?</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>file1.txt
John 1
Jane 2
Mary 3

file2.txt
1 Doe
2 Doe
3 Sue
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,13),X=e("code",null,"file1.txt",-1),ee=e("code",null,"file2.txt",-1),ne=o(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">join</span> <span class="token parameter variable">-1</span> <span class="token number">2</span> <span class="token parameter variable">-2</span> <span class="token number">1</span> file1.txt file2.txt
<span class="token comment"># 1 John Doe</span>
<span class="token comment"># 2 Jane Doe</span>
<span class="token comment"># 3 Mary Sue</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,1),ae=e("code",null,"-1",-1),se=e("code",null,"file1.txt",-1),te=e("code",null,"-2",-1),ie=e("code",null,"file2.txt",-1),oe=o(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">split</span> somefile
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This will split it into different files, by default it will split them once they reach a 1000 line limit. The files are named <code>x**</code> by default.</p><hr><h2 id="_12-sort" tabindex="-1"><a class="header-anchor" href="#_12-sort" aria-hidden="true">#</a> 12. sort</h2><p>The <code>sort</code> command is useful for sorting lines.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> file1.txt
<span class="token comment"># dog</span>
<span class="token comment"># cow</span>
<span class="token comment"># cat</span>
<span class="token comment"># elephant</span>
<span class="token comment"># bird</span>

<span class="token function">sort</span> file1.txt
<span class="token comment"># bird</span>
<span class="token comment"># cat</span>
<span class="token comment"># cow</span>
<span class="token comment"># dog</span>
<span class="token comment"># elephant</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>You can also do a reverse sort:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sort</span> <span class="token parameter variable">-r</span> file1.txt
<span class="token comment"># elephant</span>
<span class="token comment"># dog</span>
<span class="token comment"># cow</span>
<span class="token comment"># cat</span>
<span class="token comment"># bird</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>And also sort via numerical value:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sort</span> <span class="token parameter variable">-n</span> file1.txt
<span class="token comment"># bird</span>
<span class="token comment"># cat</span>
<span class="token comment"># cow</span>
<span class="token comment"># elephant</span>
<span class="token comment"># dog</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The real power of sort comes with its ability to be combined with other commands, try the following command and see what happens?</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> /etc <span class="token operator">|</span> <span class="token function">sort</span> <span class="token parameter variable">-rn</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><hr><h2 id="_13-tr-translate" tabindex="-1"><a class="header-anchor" href="#_13-tr-translate" aria-hidden="true">#</a> 13. <code>tr</code> (Translate)</h2><p>The <code>tr</code> (translate) command allows you to translate a set of characters into another set of characters. Let&#39;s try an example of translating all lower case characters to uppercase characters.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tr</span> a-z A-Z
<span class="token comment"># hello</span>
<span class="token comment"># HELLO</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see we made the ranges of <code>a-z</code> into <code>A-Z</code> and all text we type that is lowercase gets uppercased.</p><p>Try the following command what happens?</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tr</span> <span class="token parameter variable">-d</span> ello
<span class="token comment"># hello</span>
<span class="token comment"># h</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><hr><h2 id="_14-uniq-unique" tabindex="-1"><a class="header-anchor" href="#_14-uniq-unique" aria-hidden="true">#</a> 14. <code>uniq</code> (Unique)</h2><p>The <code>uniq</code> (unique) command is another useful tool for parsing text.</p><p>Let&#39;s say you had a file with lots of duplicates:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> reading.txt
<span class="token comment"># book</span>
<span class="token comment"># book</span>
<span class="token comment"># paper</span>
<span class="token comment"># paper</span>
<span class="token comment"># article</span>
<span class="token comment"># article</span>
<span class="token comment"># magazine</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>And you wanted to remove the duplicates, well you can use the uniq command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">uniq</span> reading.txt
<span class="token comment"># book</span>
<span class="token comment"># paper</span>
<span class="token comment"># article</span>
<span class="token comment"># magazine</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Let&#39;s get the count of how many occurrences of a line:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">uniq</span> <span class="token parameter variable">-c</span> reading.txt
<span class="token comment"># 2 book</span>
<span class="token comment"># 2 paper</span>
<span class="token comment"># 2 article</span>
<span class="token comment"># 1 magazine</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Let&#39;s just get unique values:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">uniq</span> <span class="token parameter variable">-u</span> reading.txt
<span class="token comment"># magazine</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Let&#39;s just get duplicate values:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">uniq</span> <span class="token parameter variable">-d</span> reading.txt
<span class="token comment"># book</span>
<span class="token comment"># paper</span>
<span class="token comment"># article</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Note</strong>: uniq does not detect duplicate lines unless they are adjacent. For eg:</p><p>Let&#39;s say you had a file with duplicates which are not adjacent:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> reading.txt
<span class="token comment"># book</span>
<span class="token comment"># paper</span>
<span class="token comment"># book</span>
<span class="token comment"># paper</span>
<span class="token comment"># article</span>
<span class="token comment"># magazine</span>
<span class="token comment"># article</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">uniq</span> reading.txt
<span class="token comment"># book</span>
<span class="token comment"># paper</span>
<span class="token comment"># book</span>
<span class="token comment"># paper</span>
<span class="token comment"># article</span>
<span class="token comment"># magazine</span>
<span class="token comment"># article</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The result returned by uniq will contain all the entries unlike the very first example.</p><p>To overcome this limitation of uniq we can use sort in combination with uniq:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sort</span> reading.txt <span class="token operator">|</span> <span class="token function">uniq</span>
<span class="token comment"># article</span>
<span class="token comment"># book</span>
<span class="token comment"># magazine</span>
<span class="token comment"># paper</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>What result would you get if you tried <code>uniq -uc</code>?</p><hr><h2 id="_15-wc-and-nl" tabindex="-1"><a class="header-anchor" href="#_15-wc-and-nl" aria-hidden="true">#</a> 15. <code>wc</code> and <code>nl</code></h2><p>The <code>wc</code> (word count) command shows the total count of words in a file.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">wc</span> /etc/passwd
<span class="token comment"># 96     265    5925 /etc/passwd</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>It display the number of lines, number of words and number of bytes, respectively.</p><p>To just see just the count of a certain field, use the <code>-l</code>, <code>-w</code>, or <code>-c</code> respectively.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">wc</span> <span class="token parameter variable">-l</span> /etc/passwd
<span class="token comment"># 96</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Another command you can use to check the count of lines on a file is the <code>nl</code> (number lines) command.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>file1.txt
<span class="token comment"># i</span>
<span class="token comment"># like</span>
<span class="token comment"># turtles</span>

<span class="token function">nl</span> file1.txt
<span class="token comment"># 1. i</span>
<span class="token comment"># 2. like</span>
<span class="token comment"># 3. turtles</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>How would you get the total count of lines by using the <code>nl</code> file without searching through the entire output? <strong>Hint</strong>: Use some of the other commands you learned in this course.</p><hr><h2 id="_16-grep" tabindex="-1"><a class="header-anchor" href="#_16-grep" aria-hidden="true">#</a> 16. <code>grep</code></h2><p>The <code>grep</code> command is quite possibly the most common text processing command you will use. It allows you to search files for characters that match a certain pattern. What if you wanted to know if a file existed in a certain directory or if you wanted to see if a string was found in a file? You certainly wouldn&#39;t dig through every line of text, you would use <code>grep</code>!</p>`,53),le=e("code",null,"sample.txt",-1),de=o(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">grep</span> fox sample.txt
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,1),ce=e("code",null,"sample.txt",-1),re=o(`<p>You can also grep patterns that are case insensitive with the <code>-i</code> flag:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">grep</span> <span class="token parameter variable">-i</span> somepattern somefile
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>To get even more flexible with grep you can combine it with other commands with <code>|</code>.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">env</span> <span class="token operator">|</span> <span class="token function">grep</span> <span class="token parameter variable">-i</span> User
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>As you can see grep is pretty versatile. You can even use regular expressions in your pattern:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> /somedir <span class="token operator">|</span> <span class="token function">grep</span> <span class="token string">&#39;.txt$&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Should return all files ending with <code>.txt</code> in somedir.</p><p>You may have heard of <code>egrep</code> or <code>fgrep</code>, these are deprecated grep calls and have since been replaced by <code>grep -E</code> and <code>grep -F</code>. Read the <code>grep</code> manpage to learn more.</p>`,8);function pe(d,ue){const t=l("router-link"),r=l("VPCard"),s=l("FontIcon");return u(),h("div",null,[e("h1",g,[f,n(" "+c(d.$frontmatter.title)+" 관련",1)]),e("blockquote",null,[e("p",null,c(d.$frontmatter.description),1)]),e("nav",k,[e("ul",null,[e("li",null,[a(t,{to:"#_1-stdout-standard-out"},{default:i(()=>[n("1. stdout (Standard Out)")]),_:1})]),e("li",null,[a(t,{to:"#_2-stdin-standard-in"},{default:i(()=>[n("2. stdin (Standard In)")]),_:1})]),e("li",null,[a(t,{to:"#_3-stderr-standard-error"},{default:i(()=>[n("3. stderr (Standard Error)")]),_:1})]),e("li",null,[a(t,{to:"#_4-pipe-and-tee"},{default:i(()=>[n("4. pipe and tee")]),_:1})]),e("li",null,[a(t,{to:"#_5-env-environment"},{default:i(()=>[n("5. env (Environment)")]),_:1})]),e("li",null,[a(t,{to:"#_6-cut"},{default:i(()=>[n("6. cut")]),_:1})]),e("li",null,[a(t,{to:"#_7-paste"},{default:i(()=>[n("7. paste")]),_:1})]),e("li",null,[a(t,{to:"#_8-head"},{default:i(()=>[n("8. head")]),_:1})]),e("li",null,[a(t,{to:"#_9-tail"},{default:i(()=>[n("9. tail")]),_:1})]),e("li",null,[a(t,{to:"#_10-expand-and-unexpand"},{default:i(()=>[n("10. expand and unexpand")]),_:1})]),e("li",null,[a(t,{to:"#_11-join-and-split"},{default:i(()=>[n("11. join and split")]),_:1})]),e("li",null,[a(t,{to:"#_12-sort"},{default:i(()=>[n("12. sort")]),_:1})]),e("li",null,[a(t,{to:"#_13-tr-translate"},{default:i(()=>[n("13. tr (Translate)")]),_:1})]),e("li",null,[a(t,{to:"#_14-uniq-unique"},{default:i(()=>[n("14. uniq (Unique)")]),_:1})]),e("li",null,[a(t,{to:"#_15-wc-and-nl"},{default:i(()=>[n("15. wc and nl")]),_:1})]),e("li",null,[a(t,{to:"#_16-grep"},{default:i(()=>[n("16. grep")]),_:1})])])]),x,a(r,m(v({title:"Text-Fu",desc:"Learn basic text manipulation and navigation.",link:"https://linuxjourney.com/lesson/stdout-standard-out-redirect",logo:"https://linuxjourney.com/assets/text-fu-622e3761a4638fdc72b7c21d2e6d41ae71861da119bdafe677a9bafa0627f1ca.png",color:"rgba(24, 188, 156, 0.2)"})),null,16),w,e("p",null,[n("Well let's say I didn't want to overwrite my "),a(s,{icon:"iconfont icon-file"}),y,n(", luckily there is a redirection operator for that as well, "),_,n(":")]),T,e("p",null,[n("This will append Hello World to the end of the "),a(s,{icon:"iconfont icon-file"}),q,n(" file, if the file doesn't already exist it will create it for us like it did with the "),W,n(" redirector!")]),j,e("p",null,[n("Normally in the cat command, you send a file to it and that file becomes the "),I,n(", in this case, we redirected "),a(s,{icon:"iconfont icon-file"}),A,n(" to be our stdin. Then the output of cat "),a(s,{icon:"iconfont icon-file"}),H,n(" which would be Hello World gets redirected to another file called "),a(s,{icon:"iconfont icon-file"}),L,n(".")]),S,e("p",null,[n("You should see just the stderr messages in "),a(s,{icon:"iconfont icon-file"}),B,n(".")]),e("p",null,[n("Now what if I wanted to see both stderr and stdout in the "),a(s,{icon:"iconfont icon-file"}),Y,n(" file? It's possible to do this with file descriptors as well:")]),P,e("p",null,[n("This sends the results of "),z,n(),N,n(" to the "),a(s,{icon:"iconfont icon-file"}),$,n(" file and then it redirects "),E,n(" to the stdout via "),O,n(". The order of operations here matters, "),D,n(" sends stderr to whatever stdout is pointing to. In this case stdout is pointing to a file, so "),J,n(" also sends stderr to a file. So if you open up that "),a(s,{icon:"iconfont icon-file"}),F,n(" file you should see both "),U,n(" and "),V,n(". In our case, the above command only outputs stderr.")]),C,e("p",null,[n("Now what if I don't want any of that cruft and want to get rid of stderr messages completely? Well you can also redirect output to a special file call "),a(s,{icon:"iconfont icon-file"}),M,n(" and it will discard any input.")]),R,e("p",null,[n("You should see the output of ls on your screen and if you open up the "),a(s,{icon:"iconfont icon-file"}),Z,n(" file you should see the same information!")]),G,e("p",null,[n("In our lesson on the cut command, we had our "),a(s,{icon:"iconfont icon-file"}),K,n(" file that contained a tab. Normally TABs would usually show a noticeable difference but some text files don't show that well enough. Having TABs in a text file may not be the desired spacing you want. To change your TABs to spaces, use the expand command.")]),Q,e("p",null,[n("To join this file you need to specify which fields you are joining, in this case we want field 2 on "),a(s,{icon:"iconfont icon-file"}),X,n(" and field 1 on "),a(s,{icon:"iconfont icon-file"}),ee,n(", so the command would look like this:")]),ne,e("p",null,[ae,n(" refers to "),a(s,{icon:"iconfont icon-file"}),se,n(" and "),te,n(" refers to "),a(s,{icon:"iconfont icon-file"}),ie,n(". Pretty neat. You can also split a file up into different files with the split command:")]),oe,e("p",null,[n("Let's use our "),a(s,{icon:"iconfont icon-file"}),le,n(" file as an example:")]),de,e("p",null,[n("You should see that grep found fox in the "),a(s,{icon:"iconfont icon-file"}),ce,n(" file.")]),re])}const me=p(b,[["render",pe],["__file","01c-text-fu.html.vue"]]);export{me as default};
