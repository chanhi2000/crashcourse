import{_ as u,r as i,o as h,c as m,a as e,b as a,t as d,d as o,w as n,n as p,g as y,e as l}from"./app-d223f2a5.js";const f={},b={id:"frontmatter-title-관련",tabindex:"-1"},v=e("a",{class:"header-anchor",href:"#frontmatter-title-관련","aria-hidden":"true"},"#",-1),g={class:"table-of-contents"},w=e("hr",null,null,-1),k=l(`<hr><h2 id="_1-overview-of-the-kernel" tabindex="-1"><a class="header-anchor" href="#_1-overview-of-the-kernel" aria-hidden="true">#</a> 1. Overview of the Kernel</h2><p>As you&#39;ve learned up to this point, the kernel is the core of the operating system. We&#39;ve talked about the other parts of the operating system but have yet to show how they all work together. The Linux operating system can be organized into three different levels of abstraction.</p><p>The most basic level is hardware, this includes our CPU, memory, hard disks, networking ports, etc. The physical layer that actually computes what our machine is doing.</p><p>The next level is the kernel, which handles process and memory management, device communication, system calls, sets up our filesystem, etc. The kernel&#39;s job is to talk to the hardware to make sure it does what we want our processes to do.</p><p>And the level that you are familiar with is the user space, the user space includes the shell, the programs that you run, the graphics, etc.</p><p>In this course, we&#39;ll be focusing on the kernel and learning its intricacies.</p><hr><h2 id="_2-privilege-levels" tabindex="-1"><a class="header-anchor" href="#_2-privilege-levels" aria-hidden="true">#</a> 2. Privilege Levels</h2><p>The next few lessons get pretty theoretical, so if you&#39;re looking for some practical stuff you can skip ahead and come back later.</p><p>Why do we have different abstraction layers for user space and kernel? Why can&#39;t you combine both powers into one layer? Well there is a very good reason why these two layers exist separately. They both operate in different modes, the kernel operates in kernel mode and the user space operates in user mode.</p><p>In kernel mode, the kernel has complete access to the hardware, it controls everything. In user space mode, there is a very small amount of safe memory and CPU that you are allowed to access. Basically, when we want to do anything that involves hardware, reading data from our disks, writing data to our disks, controlling our network, etc, it is all done in kernel mode. Why is this necessary? Imagine if your machine was infected with spyware, you wouldn&#39;t want it to be able to have direct access to your system&#39;s hardware. It can access all your data, your webcam, etc. and that&#39;s no good.</p><p>These different modes are called privilege levels (aptly named for the levels of privilege you get) and are often described as protection rings. To make this picture easier to paint, let&#39;s say you find out that Britney Spears is in town at your local klerb, she&#39;s protected by her groupies, then her personal bodyguards, then the bouncer outside the klerb. You want to get her autograph (because why not?), but you can&#39;t get to her because she is heavily protected. The rings work the same way, the innermost ring corresponds to the highest privilege level. There are two main levels or modes in an x86 computer architecture. Ring #3 is the privilege that user mode applications run in, Ring #0 is the privilege that the kernel runs in. Ring #0 can execute any system instruction and is given full trust. So now that we know how those privilege levels work, how are we able to write anything to our hardware? Won&#39;t we always be in a different mode than the kernel?</p><p>The answer is with system calls, system calls allow us to perform a privileged instruction in kernel mode and then switch back to user mode.</p><hr><h2 id="_3-system-calls" tabindex="-1"><a class="header-anchor" href="#_3-system-calls" aria-hidden="true">#</a> 3. System Calls</h2><p>Remember Britney in the previous lesson? Let&#39;s say we want to see her and get some drinks together, how do we get from standing outside in the crowds of people to inside her innermost circle? We would use system calls. System calls are like the VIP passes that get you to a secret side door that leads directly to Britney.</p><p>System calls (<code>syscall</code>) provide user space processes a way to request the kernel to do something for us. The kernel makes certain services available through the system call API. These services allow us to read or write to a file, modify memory usage, modify our network, etc. The amount of services are fixed, so you can&#39;t be adding system calls nilly willy, your system already has a table of what system calls exist and each system call has a unique ID.</p><p>I won&#39;t get into specifics of system calls, as that will require you to know a bit of C, but the basics is that when you call a program like ls, the code inside this program contains a system call wrapper (so not the actual system call yet). Inside this wrapper it invokes the system call which will execute a trap, this trap then gets caught by the system call handler and then references the system call in the system call table. Let&#39;s say we are trying to call the <code>stat()</code> system call, it&#39;s identified by a syscall ID and the purpose of the <code>stat()</code> system call is to query the status of a file. Now remember, you were running the ls program in non-privilege mode. So now it sees you&#39;re trying to make a <code>syscall</code>, it then switches you over to kernel mode, there it does lots of things but most importantly it looks up your syscall number, finds it in a table based on the syscall ID and then executes the function you wanted to run. Once it&#39;s done, it will return back to user mode and your process will receive a return status if it was successful or if it had an error. The inner workings of syscalls get really detailed, I would recommend looking at information online if you want to learn more.</p><p>You can actually view the system calls that a process makes with the strace command. The strace command is useful for debugging how a program executed.</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>strace ls
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><hr><h2 id="_4-kernel-installation" tabindex="-1"><a class="header-anchor" href="#_4-kernel-installation" aria-hidden="true">#</a> 4. Kernel Installation</h2><p>Ok, now that we&#39;ve got all that boring stuff out of the way, let&#39;s talk about actually installing and modifying kernels. You can install multiple kernels on your system, remember in our lesson on the boot process? In our GRUB menu we can choose which kernel to boot to.</p><p>To see what kernel version you have on your system, use the following command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">uname</span> <span class="token parameter variable">-r</span>
<span class="token comment"># 3.19.0-43-generic</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>The <code>uname</code> command prints system information, the <code>-r</code> command will print out all of the kernel release version.</p><p>You can install the Linux kernel in different ways, you can download the source package and compile from source or you can install it using package management tools.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">apt</span> <span class="token function">install</span> linux-generic-lts-vivid
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>and then just reboot into the kernel you installed. Simple right? Kind of, you&#39;ll need to also install other linux packages such as the linux-headers, linux-image-generic, etc). You can also specify the version number, so the above command can look like, <code>sudo apt install 3.19.0-43-generic</code></p><p>Alternatively, if you just want the updated kernel version, just use <code>dist-upgrade</code>, it performs upgrades to all package on your system:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">apt</span> dist-upgrade
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>There are many different kernel versions, some are used as LTS (<em>long term support</em>), some are the latest and greatest, the compatibility may be very different between kernel versions so you may want to try out different kernels.</p><ol><li>Find out what kernel version you have.</li><li>Research the different versions of kernels available</li></ol><hr><h2 id="_5-kernel-location" tabindex="-1"><a class="header-anchor" href="#_5-kernel-location" aria-hidden="true">#</a> 5. Kernel Location</h2>`,36),_=e("code",null,"/boot",-1),x=e("p",null,"You will see multiple files for different kernel versions:",-1),T=e("ul",null,[e("li",null,[e("code",null,"vmlinuz"),a(" - this is the actual linux kernel")]),e("li",null,[e("code",null,"initrd"),a(" - as we've discussed before, the initrd is used as a temporary file system, used before loading the kernel")]),e("li",null,[e("code",null,"System.map"),a(" - symbolic lookup table")]),e("li",null,[e("code",null,"config"),a(" - kernel configuration settings, if you are compiling your own kernel, you can set which modules can be loaded")])],-1),I=e("code",null,"/boot",-1),L=l(`<p>Go into your boot directory and see what files are in there.</p><hr><h2 id="_6-kernel-modules" tabindex="-1"><a class="header-anchor" href="#_6-kernel-modules" aria-hidden="true">#</a> 6. Kernel Modules</h2><p>Let&#39;s say I have a sweet ride, I invest a lot of time and money into it. I add a spoiler, hitch, bike rack and other random things. These components don&#39;t actually change the core functionality of the car and I can remove and add them very easily. The kernel uses the same concept with kernel modules.</p><p>The kernel in itself is a monolithic piece of software, when we want to add support for a new type of keyboard, we don&#39;t write this code directly into the kernel code. Just as we wouldn&#39;t meld a bike rack to our car (well maybe some people would do that). Kernel modules are pieces of code that can be loaded and unloaded into the kernel on demand. They allow us to extend the functionality of the kernel without actually adding to the core kernel code. We can also add modules and not have to reboot the system (in most cases).</p><h3 id="view-a-list-of-currently-loaded-modules" tabindex="-1"><a class="header-anchor" href="#view-a-list-of-currently-loaded-modules" aria-hidden="true">#</a> View a list of currently loaded modules</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>lsmod
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="load-a-module" tabindex="-1"><a class="header-anchor" href="#load-a-module" aria-hidden="true">#</a> Load a module</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> modprobe bluetooth
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,9),K=e("code",null,"/lib/modules/(kernel version)/kernel/drivers",-1),S=l(`<h3 id="remove-a-module" tabindex="-1"><a class="header-anchor" href="#remove-a-module" aria-hidden="true">#</a> Remove a module</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> modprobe <span class="token parameter variable">-r</span> bluetooth
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="load-on-bootup" tabindex="-1"><a class="header-anchor" href="#load-on-bootup" aria-hidden="true">#</a> Load on bootup</h3>`,3),P=e("code",null,"/etc/modprobe.d",-1),W=l(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>/etc/modprobe.d/peanutbutter.conf
<span class="token comment"># options peanut_butter type=almond</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>A bit of a outlandish example, but if you had a module named peanut_butter and you wanted to add a kernel parameter for type=almond, you can have it load on startup using this configuration file. Also note that kernel modules have their own kernel parameters so you&#39;ll want to read about the module specifically to find out more.</p><h3 id="do-not-load-on-bootup" tabindex="-1"><a class="header-anchor" href="#do-not-load-on-bootup" aria-hidden="true">#</a> Do not load on bootup</h3><p>You can also make sure a module does not load on bootup by adding a configuration file like so:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>/etc/modprobe.d/peanutbutter.conf
<span class="token comment"># blacklist peanut_butter</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Unload your bluetooth module with modprobe and see what happens. How will you fix this?</p>`,6);function C(r,R){const t=i("router-link"),c=i("VPCard"),s=i("FontIcon");return h(),m("div",null,[e("h1",b,[v,a(" "+d(r.$frontmatter.title)+" 관련",1)]),e("blockquote",null,[e("p",null,d(r.$frontmatter.description),1)]),e("nav",g,[e("ul",null,[e("li",null,[o(t,{to:"#_1-overview-of-the-kernel"},{default:n(()=>[a("1. Overview of the Kernel")]),_:1})]),e("li",null,[o(t,{to:"#_2-privilege-levels"},{default:n(()=>[a("2. Privilege Levels")]),_:1})]),e("li",null,[o(t,{to:"#_3-system-calls"},{default:n(()=>[a("3. System Calls")]),_:1})]),e("li",null,[o(t,{to:"#_4-kernel-installation"},{default:n(()=>[a("4. Kernel Installation")]),_:1})]),e("li",null,[o(t,{to:"#_5-kernel-location"},{default:n(()=>[a("5. Kernel Location")]),_:1})]),e("li",null,[o(t,{to:"#_6-kernel-modules"},{default:n(()=>[a("6. Kernel Modules")]),_:1}),e("ul",null,[e("li",null,[o(t,{to:"#view-a-list-of-currently-loaded-modules"},{default:n(()=>[a("View a list of currently loaded modules")]),_:1})]),e("li",null,[o(t,{to:"#load-a-module"},{default:n(()=>[a("Load a module")]),_:1})]),e("li",null,[o(t,{to:"#remove-a-module"},{default:n(()=>[a("Remove a module")]),_:1})]),e("li",null,[o(t,{to:"#load-on-bootup"},{default:n(()=>[a("Load on bootup")]),_:1})]),e("li",null,[o(t,{to:"#do-not-load-on-bootup"},{default:n(()=>[a("Do not load on bootup")]),_:1})])])])])]),w,o(c,p(y({title:"Kernel",desc:"The most important part of the Linux system, learn about how it works and how to configure it.",link:"https://linuxjourney.com/lesson/kernel-overview",logo:"https://linuxjourney.com/assets/kernel-99533d3ebf83d9bb34e8971887d90acb259f72d0092aa1f628d6c7036bd62144.png",color:"rgba(24, 188, 156, 0.2)"})),null,16),k,e("p",null,[a("What happens when you install a new kernel? Well it actually adds a couple of files to your system, these files are usually added to the "),o(s,{icon:"iconfont icon-folder"}),_,a(" directory.")]),x,T,e("p",null,[a("If your "),o(s,{icon:"iconfont icon-folder"}),I,a(" directory runs out of space, you can always delete old versions of these files or just use a package manager, but be careful when doing maintenance in this directory and don't accidentally delete the kernel you are using.")]),L,e("p",null,[a("Modprobe loads tries the module from "),o(s,{icon:"iconfont icon-folder"}),K,a(". Kernel modules may also have dependencies, modprobe loads our module dependencies if they are not already loaded.")]),S,e("p",null,[a("You can also load modules during system boot, instead of temporarily loading them with modprobe (which will be unloaded when you reboot). Just modify the "),o(s,{icon:"iconfont icon-folder"}),P,a(" directory and add a configuration file in it like so:")]),W])}const B=u(f,[["render",C],["__file","02d-kernel.html.vue"]]);export{B as default};
