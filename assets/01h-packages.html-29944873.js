import{_ as h,r as o,o as u,c as m,a as e,b as a,t as d,d as s,w as n,n as g,g as f,e as i}from"./app-d223f2a5.js";const k={},b={id:"frontmatter-title-관련",tabindex:"-1"},v=e("a",{class:"header-anchor",href:"#frontmatter-title-관련","aria-hidden":"true"},"#",-1),y={class:"table-of-contents"},w=e("hr",null,null,-1),_=i('<hr><h2 id="_1-software-distribution" tabindex="-1"><a class="header-anchor" href="#_1-software-distribution" aria-hidden="true">#</a> 1. Software Distribution</h2><p>Your system is comprised of many packages such as internet browsers, text editors, media players, etc. These packages are managed via package managers, which install and maintain the software on your system. Not all packages are installed through package managers though, you can commonly install packages directly from their source code (we&#39;ll get to that soon). However the majority of the time you will use a package manager to install software, the most common variety of packages are Debian (<code>.deb</code>) and Red Hat (<code>.rpm</code>). Debian style packages are used in distributions such as Debian, Ubuntu, LinuxMint, etc. Red Hat style packages are seen in Red Hat Enterprise Linux, Fedora, CentOS, etc.</p><p>What are packages? You may know them as Chrome, Photoshop, etc and they are, but what they really are just lots and lots of files that have been compiled into one. The people (or sometimes a single person) that write this software are known as <strong>upstream providers</strong>, they compile their code and write up how to get it installed. These upstream providers work on getting out new software and update existing software. When they are ready to release it to the world, they send their package to <strong>package maintainers</strong>, who handle getting this piece of software in the hands of the users. These package maintainers review, manage and distribute this software in the form of packages.</p><hr><h2 id="_2-package-repositories" tabindex="-1"><a class="header-anchor" href="#_2-package-repositories" aria-hidden="true">#</a> 2. Package Repositories</h2><p>How do packages that get uploaded to the internet somehow end up on our computers? Do you go to the download page of each package you want and click download and install? Well, actually you can do that, but there is something better called package repositories. Repositories are just a central storage location for packages. There are tons of repositories that hold lots of packages and best of all they are all found on the internet, no silly installation disks. Your machine doesn&#39;t know where to look for these repositories unless you explicitly tell it where to look.</p>',7),x={href:"http://download.widgets/linux/deb/",target:"_blank",rel:"noopener noreferrer"},z=e("p",null,"Now instead of going to their website to download the package directly, you can tell your machine to find WackyWidgets software from the source link.",-1),R=e("code",null,"/etc/apt/sources.list",-1),T=i(`<hr><h2 id="_3-tar-and-gzip" tabindex="-1"><a class="header-anchor" href="#_3-tar-and-gzip" aria-hidden="true">#</a> 3. <code>tar</code> and <code>gzip</code></h2><p>Before we get into package installation and the different managers, we need to discuss archiving and compressing files, because you will most likely encounter these when you hunt for software on the internet.</p><p>You probably already know what a file archive is, you&#39;ve most likely encountered file types such as <code>.rar</code> and <code>.zip</code>. These are an archive of files, they contain many files inside of them, but they come in this very neat single file known as an archive.</p><h3 id="compressing-files-with-gzip" tabindex="-1"><a class="header-anchor" href="#compressing-files-with-gzip" aria-hidden="true">#</a> Compressing files with <code>gzip</code></h3><p>gzip is program used to compress files in Linux, they end in a <code>.gz</code> extension.</p><p>To compress a file down:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">gzip</span> mycoolfile
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>To decompress the file:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>gunzip mycoolfile.gz
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="creating-archives-with-tar" tabindex="-1"><a class="header-anchor" href="#creating-archives-with-tar" aria-hidden="true">#</a> Creating archives with <code>tar</code></h3><p>Unfortunately, gzip can&#39;t add multiple files into one archive for us. Luckily we have the tar program which does. When you create an archive using tar, it will have a <code>.tar</code> extension.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tar</span> cvf mytarfile.tar mycoolfile1 mycoolfile2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li><code>c</code> - create</li><li><code>v</code> - tell the program to be verbose and let us see what it&#39;s doing</li><li><code>f</code> - the filename of the tar file has to come after this option, if you are creating a tar file you&#39;ll have to come up with a name</li></ul><h3 id="unpacking-archives-with-tar" tabindex="-1"><a class="header-anchor" href="#unpacking-archives-with-tar" aria-hidden="true">#</a> Unpacking archives with <code>tar</code></h3><p>To extract the contents of a tar file, use:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tar</span> xvf mytarfile.tar
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li><code>x</code> - extract</li><li><code>v</code> - tell the program to be verbose and let us see what it&#39;s doing</li><li><code>f</code> - the file you want to extract</li></ul><h3 id="compressing-uncompressing-archives-with-tar-and-gzip" tabindex="-1"><a class="header-anchor" href="#compressing-uncompressing-archives-with-tar-and-gzip" aria-hidden="true">#</a> Compressing/uncompressing archives with <code>tar</code> and <code>gzip</code></h3><p>Many times you&#39;ll see a tar file that has been compressed such as: <code>mycompressedarchive.tar.gz</code>, all you need to do is work outside in, so first remove the compression with gunzip and then you can unpack the tar file. Or you can alternatively use the <code>z</code> option with <code>tar</code>, which just tells it to use the <code>gzip</code> or gunzip utility.</p><p>Create a compressed tar file:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tar</span> czf myfile.tar.gz
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Uncompress and unpack:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">tar</span> xzf file.tar
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>If you need help remember this: e <strong>X</strong> tract all <strong>Z</strong> ee <strong>F</strong> iles!</p>`,25),D=e("code",null,"tar",-1),P={href:"https://xkcd.com/1168",target:"_blank",rel:"noopener noreferrer"},C=i(`<h3 id="other-utilities" tabindex="-1"><a class="header-anchor" href="#other-utilities" aria-hidden="true">#</a> Other Utilities</h3><p>Throughout your journey of Linux, you&#39;ll encounter other archive and compression types such as: <code>bzip2</code>, <code>compress</code>, <code>zip</code>, <code>unzip</code>, etc. They are a little less common, but just keep in mind that different utilities will call for different commands.</p><p>Familiarize yourself with the tar documentation and look at the other options available in the manpage.</p><hr><h2 id="_4-package-dependencies" tabindex="-1"><a class="header-anchor" href="#_4-package-dependencies" aria-hidden="true">#</a> 4. Package Dependencies</h2><p>Packages very rarely work by themselves, they are most often accompanied by dependencies to help them run. For example, let&#39;s say we have a group of restaurants, these restaurants all make different cuisine, however they all get their ingredients from the same farm. Their food is dependent on the farm&#39;s supplies, if the farm were to suddenly stop supplying food, well then the restaurants would be in a pretty bad state.</p><p>In Linux, these dependencies are often other packages or shared libraries. Shared libraries are libraries of code that other programs want to use and don&#39;t want to have to rewrite for themselves. Think of the restaurant again, how much work would it be if every restaurant also farmed their own food? Too much.</p><p>We will dig more into shared libraries in the filesystem course, so for now just remember that packages have dependencies to help them run, whether those dependencies are other packages or libraries, if the dependencies aren&#39;t there the package will end up in a broken state and most of the time not even install.</p><hr><h2 id="_5-rpm-and-dpkg" tabindex="-1"><a class="header-anchor" href="#_5-rpm-and-dpkg" aria-hidden="true">#</a> 5. <code>rpm</code> and <code>dpkg</code></h2><p>Although most of this course is about package management systems (the Batmans of package management), we mustn&#39;t forget about the Robins. Although very useful and reliable, they don&#39;t come with that sweet batmobile and utility belt.</p><p>Just like .exe is a single executable file, so is <code>.deb</code> and <code>.rpm</code>. You normally wouldn&#39;t see these if you use package repositories, but if you directly download packages, you will most likely get them in these popular format. Obviously, they are exclusive to their distributions, <code>.deb</code> for Debian based and .rpm for Red Hat based.</p><p>To install these direct packages, you can use the package management commands: rpm and dpkg. These tools are used to install package files, however they will not install the package dependencies, so if your package had 10 dependencies, you would have to install those packages separately and then their dependencies and so on and so forth. As you can see, that was one of the reasons that brought forth the full blown management systems that we will discuss this later.</p><p>Keep in mind that there will be countless times when you need to install, query or verify a package with one of these tools, so remember these commands.</p><h3 id="install-a-package" tabindex="-1"><a class="header-anchor" href="#install-a-package" aria-hidden="true">#</a> Install a package</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>dpkg <span class="token parameter variable">-i</span> some_deb_package.deb <span class="token comment"># Debian</span>
<span class="token function">rpm</span> <span class="token parameter variable">-i</span> some_rpm_package.rpm <span class="token comment"># RPM</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>The <code>i</code> stands for install. You can also use the longer format of <code>--install</code>.</p><h3 id="remove-a-package" tabindex="-1"><a class="header-anchor" href="#remove-a-package" aria-hidden="true">#</a> Remove a package</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>dpkg <span class="token parameter variable">-r</span> some_deb_package.deb <span class="token comment"># Debian</span>
<span class="token function">rpm</span> <span class="token parameter variable">-e</span> some_rpm_package.rpm <span class="token comment"># RPM</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Debian: <strong>r</strong> for remove RPM: <strong>e</strong> for erase</p><h3 id="list-installed-packages" tabindex="-1"><a class="header-anchor" href="#list-installed-packages" aria-hidden="true">#</a> List installed packages</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>dpkg <span class="token parameter variable">-l</span> <span class="token comment"># Debian</span>
<span class="token function">rpm</span> <span class="token parameter variable">-qa</span> <span class="token comment"># RPM</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Debian: <strong>l</strong> for list RPM: <strong>q</strong> for query and <strong>a</strong> for all</p><p>Find a program that you want to install on your system like Google Chrome and install it using one of these commands.</p><hr><h2 id="_6-yum-and-apt" tabindex="-1"><a class="header-anchor" href="#_6-yum-and-apt" aria-hidden="true">#</a> 6. <code>yum</code> and <code>apt</code></h2><p>Ah, the Batmans of package management, these systems come with all the fixins to make package installation, removal and changes easier, including installing package dependencies. Two of the most popular management systems is <code>yum</code> and <code>apt</code>. Yum is exclusive to the Red Hat family and apt is exclusively to the Debian family.</p><h3 id="install-a-package-from-a-repository" tabindex="-1"><a class="header-anchor" href="#install-a-package-from-a-repository" aria-hidden="true">#</a> Install a package from a repository</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">apt</span> <span class="token function">install</span> <span class="token operator">&lt;</span>package_name<span class="token operator">&gt;</span> <span class="token comment">#Debian</span>
yum <span class="token function">install</span> <span class="token operator">&lt;</span>package_name<span class="token operator">&gt;</span> <span class="token comment">#RPM</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="remove-a-package-1" tabindex="-1"><a class="header-anchor" href="#remove-a-package-1" aria-hidden="true">#</a> Remove a package</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">apt</span> remove <span class="token operator">&lt;</span>package_name<span class="token operator">&gt;</span> <span class="token comment">#Debian</span>
yum erase <span class="token operator">&lt;</span>package_name<span class="token operator">&gt;</span> <span class="token comment">#RPM</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="updating-packages-for-a-repository" tabindex="-1"><a class="header-anchor" href="#updating-packages-for-a-repository" aria-hidden="true">#</a> Updating packages for a repository</h3><p>It&#39;s always best practice to update your package repositories so they are up to date before you install and update a package.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">apt</span> update<span class="token punctuation">;</span> <span class="token function">apt</span> upgrade <span class="token comment">#Debian</span>
yum update <span class="token comment">#RPM</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="get-information-about-an-installed-package" tabindex="-1"><a class="header-anchor" href="#get-information-about-an-installed-package" aria-hidden="true">#</a> Get information about an installed package</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>t show <span class="token operator">&lt;</span>package_name<span class="token operator">&gt;</span> <span class="token comment">#Debian</span>
m info <span class="token operator">&lt;</span>package_name<span class="token operator">&gt;</span> <span class="token comment">#RPM</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Run through each of these package commands and see the output you receive.</p><hr><h2 id="_7-compile-source-code" tabindex="-1"><a class="header-anchor" href="#_7-compile-source-code" aria-hidden="true">#</a> 7. Compile Source Code</h2><p>Often times you will encounter an obscure package that only comes in the form of pure source code. You&#39;ll need to use a few commands to get that source code package compiled and installed on your system.</p><p>First thing is first, you&#39;ll need to have software to install the tools that will allow you to compile source code.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">sudo</span> <span class="token function">apt</span> <span class="token function">install</span> build-essential
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Once you do that, extract the contents of the package file, most likely a .tar.gz file.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">tar</span> <span class="token parameter variable">-xzvf</span> package.tar.gz
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Before you do anything, take a look at the <code>README</code> or <code>INSTALL</code> file inside the package. Sometimes there will be specific installation instructions.</p><p>Depending on what compile method that the developer used, you&#39;ll have to use different commands, such as cmake or something else.</p><p>However, most commonly you&#39;ll see basic make compilation, so we&#39;ll discuss that:</p><p>Inside the package contents will be a configure script, this script checks for dependencies on your system and if you are missing anything, you&#39;ll see an error and you&#39;ll need to fix those dependencies.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ ./configure
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The <code>./</code> allows you to execute a script in the current directory.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">make</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Inside of the package contents, there is a file called Makefile that contains rules to building the software. When you run the make command, it looks at this file to build the software.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">sudo</span> <span class="token function">make</span> <span class="token function">install</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This command actually installs the package, it will copy the correct files to the correct locations on your computer.</p><p>If you want to uninstall the package, use:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">sudo</span> <span class="token function">make</span> uninstall
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Be wary when using make install, you may not realize how much is actually going on in the background. If you decide to remove this package, you may not actually remove everything because you didn&#39;t realize what was added to your system. Instead forget everything about make install that I just explained to you and use the <code>checkinstall</code> command. This command will make a .deb file for you that you can easily install and uninstall.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">sudo</span> checkinstall
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This command will essentially &quot;make install&quot; and build a <code>.deb</code> package and install it. This makes it easier to remove the package later on.</p><p>Find a source code program (from a trusted site) and install from source.</p>`,60);function I(r,W){const t=o("router-link"),c=o("VPCard"),l=o("ExternalLinkIcon"),p=o("FontIcon");return u(),m("div",null,[e("h1",b,[v,a(" "+d(r.$frontmatter.title)+" 관련",1)]),e("blockquote",null,[e("p",null,d(r.$frontmatter.description),1)]),e("nav",y,[e("ul",null,[e("li",null,[s(t,{to:"#_1-software-distribution"},{default:n(()=>[a("1. Software Distribution")]),_:1})]),e("li",null,[s(t,{to:"#_2-package-repositories"},{default:n(()=>[a("2. Package Repositories")]),_:1})]),e("li",null,[s(t,{to:"#_3-tar-and-gzip"},{default:n(()=>[a("3. tar and gzip")]),_:1}),e("ul",null,[e("li",null,[s(t,{to:"#compressing-files-with-gzip"},{default:n(()=>[a("Compressing files with gzip")]),_:1})]),e("li",null,[s(t,{to:"#creating-archives-with-tar"},{default:n(()=>[a("Creating archives with tar")]),_:1})]),e("li",null,[s(t,{to:"#unpacking-archives-with-tar"},{default:n(()=>[a("Unpacking archives with tar")]),_:1})]),e("li",null,[s(t,{to:"#compressing-uncompressing-archives-with-tar-and-gzip"},{default:n(()=>[a("Compressing/uncompressing archives with tar and gzip")]),_:1})]),e("li",null,[s(t,{to:"#other-utilities"},{default:n(()=>[a("Other Utilities")]),_:1})])])]),e("li",null,[s(t,{to:"#_4-package-dependencies"},{default:n(()=>[a("4. Package Dependencies")]),_:1})]),e("li",null,[s(t,{to:"#_5-rpm-and-dpkg"},{default:n(()=>[a("5. rpm and dpkg")]),_:1}),e("ul",null,[e("li",null,[s(t,{to:"#install-a-package"},{default:n(()=>[a("Install a package")]),_:1})]),e("li",null,[s(t,{to:"#remove-a-package"},{default:n(()=>[a("Remove a package")]),_:1})]),e("li",null,[s(t,{to:"#list-installed-packages"},{default:n(()=>[a("List installed packages")]),_:1})])])]),e("li",null,[s(t,{to:"#_6-yum-and-apt"},{default:n(()=>[a("6. yum and apt")]),_:1}),e("ul",null,[e("li",null,[s(t,{to:"#install-a-package-from-a-repository"},{default:n(()=>[a("Install a package from a repository")]),_:1})]),e("li",null,[s(t,{to:"#remove-a-package-1"},{default:n(()=>[a("Remove a package")]),_:1})]),e("li",null,[s(t,{to:"#updating-packages-for-a-repository"},{default:n(()=>[a("Updating packages for a repository")]),_:1})]),e("li",null,[s(t,{to:"#get-information-about-an-installed-package"},{default:n(()=>[a("Get information about an installed package")]),_:1})])])]),e("li",null,[s(t,{to:"#_7-compile-source-code"},{default:n(()=>[a("7. Compile Source Code")]),_:1})])])]),w,s(c,g(f({title:"Packages",desc:"Learn all about the dpkg, apt-get, rpm and yum package management tools.",link:"https://linuxjourney.com/lesson/software-distribution",logo:"https://linuxjourney.com/assets/package-management-9a40f89dde6d2e2d861722093b97eadc3b852446125df64ea2ce344dc47ef00d.png",color:"rgba(24, 188, 156, 0.2)"})),null,16),_,e("p",null,[a("For example, let's say I want WackyWidgets Software on my machine. Well WackyWidgets manages their own repositories for their widget packages, inside this repository are 10 packages, the CoolWidget package, the SuperWidget package, etc. WackyWidgets hosts this repository at a source link called: "),e("a",x,[a("http://download.widgets/linux/deb/"),s(l)])]),z,e("p",null,[a("Your distribution already comes with pre-approved sources to get packages from and this is how it installs all the base packages you see on your system. On a Debian system, this sources file is the "),s(p,{icon:"iconfont icon-file"}),R,a(" file. Your machine will know to look there and check for any source repositories you added.")]),T,e("p",null,[D,a(" is one of those commands that is so important and yet you never really remember it, relevant xkcd: "),e("a",P,[a("https://xkcd.com/1168/"),s(l)])]),C])}const M=h(k,[["render",I],["__file","01h-packages.html.vue"]]);export{M as default};
