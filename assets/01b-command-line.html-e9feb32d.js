import{_ as h,r as l,o as u,c as p,a as e,b as a,t as r,d as o,w as s,n as m,g as v,e as i}from"./app-d223f2a5.js";const b={},f={id:"frontmatter-title-관련",tabindex:"-1"},g=e("a",{class:"header-anchor",href:"#frontmatter-title-관련","aria-hidden":"true"},"#",-1),y={class:"table-of-contents"},w=e("hr",null,null,-1),k=i(`<hr><h2 id="_1-the-shell" tabindex="-1"><a class="header-anchor" href="#_1-the-shell" aria-hidden="true">#</a> 1. The Shell</h2><p>The world is your oyster, or really the shell is your oyster. What is the shell? The shell is basically a program that takes your commands from the keyboard and sends them to the operating system to perform. If you’ve ever used a GUI, you’ve probably seen programs such as “Terminal” or “Console” these are just programs that launch a shell for you. Throughout this entire course we will be learning about the wonders of the shell.</p><p>In this course we will use the shell program bash (Bourne Again shell), almost all Linux distributions will default to the bash shell. There are other shells available such as ksh, zsh, tsch, but we won’t get into any of those.</p><p>Let’s jump right in! Depending on the distribution your shell prompt might change, but for the most part it should adhere to the following format:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>username@hostname:current_directory
pete@icebox:/home/pete $
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Notice the <code>$</code> at the end of the prompt? Different shells will have different prompts, in our case the <code>$</code> is for a normal user using Bash, Bourne or Korn shell, you don&#39;t add the prompt symbol when you type the command, just know that it&#39;s there.</p><p>Let’s start with a simple command, echo. The echo command just prints out the text arguments to the display.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> Hello World
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p>Try some other Linux commands and see what they output:</p></blockquote><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">date</span>
<span class="token function">whoami</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><hr><h2 id="_2-pwd-print-working-directory" tabindex="-1"><a class="header-anchor" href="#_2-pwd-print-working-directory" aria-hidden="true">#</a> 2. <code>pwd</code> (Print Working Directory)</h2><p>Everything in Linux is a file, as you journey deeper into Linux you’ll understand this, but for now just keep that in mind. Every file is organized in a hierarchical directory tree. The first directory in the filesystem is aptly named the root directory. The root directory has many folders and files which you can store more folders and files, etc. Here is an example of what the directory tree looks like:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>/
|-- bin
|   |-- file1
|   |-- file2
|-- etc
|   |-- file3
|   \`-- directory1
|       |-- file4
|       \`-- file5
|-- home
|-- var
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The location of these files and directories are referred to as paths. If you had a folder named home with a folder inside of it named pete and another folder in that folder called Movies, that path would look like this: <code>/home/pete/Movies</code>, pretty simple huh?</p><p>Navigation of the filesystem, much like real life is helpful if you know where you are and where you are going. To see where you are, you can use the pwd command, this command means “print working directory” and it just shows you which directory you are in, note the path stems from the root directory.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">pwd</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Where are you? Where am I? Give it a try.</p><hr><h2 id="_3-cd-change-directory" tabindex="-1"><a class="header-anchor" href="#_3-cd-change-directory" aria-hidden="true">#</a> 3. <code>cd</code> (Change Directory)</h2><p>Now that you know where you are, let’s see if we can move around the filesystem a bit. Remember we’ll need to navigate our way using paths. There are two different ways to specify a path, with absolute and relative paths.</p>`,22),_=e("code",null,"/",-1),x=e("code",null,"/home/pete/Desktop",-1),T=e("code",null,"/home/pete/Documents",-1),q=e("code",null,"/home/pete/Documents/taxes",-1),I=e("code",null,'<FontIcon icon="iconfont icon-folder"/>taxes/',-1),j=i(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">cd</span> /home/pete/Pictures
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,1),D=e("code",null,"/home/pete/Pictures",-1),W=i(`<p>Now from this directory I have a folder inside called Hawaii, I can navigate to that folder with:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">cd</span> Hawaii
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,2),N=e("code",null,"/home/pete/Pictures",-1),R=e("p",null,"It can get pretty tiring navigating with absolute and relative paths all the time, luckily there are some shortcuts to help you out.",-1),L=e("li",null,[e("code",null,"."),a(" (current directory). This is the directory you are currently in.")],-1),P=e("li",null,[e("code",null,".."),a(" (parent directory). Takes you to the directory above your current.")],-1),M=e("code",null,"~",-1),C=e("code",null,"/home/pete",-1),Y=e("li",null,[e("code",null,"-"),a(" (previous directory). This will take you to the previous directory you were just at.")],-1),F=i(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">cd</span> <span class="token builtin class-name">.</span>
<span class="token builtin class-name">cd</span> <span class="token punctuation">..</span>
<span class="token builtin class-name">cd</span> ~
<span class="token builtin class-name">cd</span> -
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Give them a try!</p><blockquote><p>Run the cd command without any flags, where does it take you?</p></blockquote><hr><h2 id="_4-ls-list-directories" tabindex="-1"><a class="header-anchor" href="#_4-ls-list-directories" aria-hidden="true">#</a> 4. <code>ls</code> (List Directories)</h2><p>Now that we know how to move around the system, how do we figure out what is available to us? Right now it’s like we are moving around in the dark. Well, we can use the wonderful <code>ls</code> command to list directory contents. The <code>ls</code> command will list directories and files in the current directory by default, however you can specify which path you want to list the directories of.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span>
<span class="token function">ls</span> /home/pete
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>ls</code> is a quite useful tool, it also shows you detailed information about the files and directories you are looking at.</p><p>Also note that not all files in a directory will be visible. Filenames that start with <code>.</code> are hidden, you can view them however with the ls command and pass the <code>-a</code> flag to it (a for all).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> <span class="token parameter variable">-a</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>There is also one more useful ls flag, <code>-l</code> for long, this shows a detailed list of files in a long format. This will show you detailed information, starting from the left: file permissions, number of links, owner name, owner group, file size, timestamp of last modification, and file/directory name.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> <span class="token parameter variable">-l</span>
<span class="token comment"># total 80</span>
<span class="token comment"># drwxr-x--- 7 pete penguingroup   4096 Nov 20 16:37 Desktop</span>
<span class="token comment"># drwxr-x--- 2 pete penguingroup   4096 Oct 19 10:46  Documents</span>
<span class="token comment"># drwxr-x--- 4 pete penguingroup   4096 Nov 20 09:30 Downloads</span>
<span class="token comment"># drwxr-x--- 2 pete penguingroup   4096 Oct  7 13:13   Music</span>
<span class="token comment"># drwxr-x--- 2 pete penguingroup   4096 Sep 21 14:02 Pictures</span>
<span class="token comment"># drwxr-x--- 2 pete penguingroup   4096 Jul 27 12:41   Public</span>
<span class="token comment"># drwxr-x--- 2 pete penguingroup   4096 Jul 27 12:41   Templates</span>
<span class="token comment"># drwxr-x--- 2 pete penguingroup   4096 Jul 27 12:41   Videos</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Commands have things called flags (or arguments or options, whatever you want to call it) to add more functionality. See how we added <code>-a</code> and <code>-l</code>, well you can add them both together with <code>-la</code>. The order of the flags determines which order it goes in, most of the time this doesn’t really matter so you can also do <code>ls -al</code> and it would still work.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ls</span> <span class="token parameter variable">-la</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Run ls with different flags and see the output you receive.</p><ul><li><code>ls -R</code>: recursively list directory contents</li><li><code>ls -r</code>: reverse order while sorting</li><li><code>ls -t</code>: sort by modification time, newest first</li></ul><hr><h2 id="_5-touch" tabindex="-1"><a class="header-anchor" href="#_5-touch" aria-hidden="true">#</a> 5. <code>touch</code></h2><p>Let’s learn how to make some files. A very simple way is to use the touch command. Touch allows you to the create new empty files.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">touch</span> mysuperduperfile
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>And boom, new file!</p><p>Touch is also used to change timestamps on existing files and directories. Give it a try, do an <code>ls -l</code> on a file and note the timestamp, then touch that file and it will update the timestamp.</p><p>There are many other ways to create files that involve other things like redirection and text editors, but we’ll get to that in the Text Manipulation course.</p><blockquote><p>Exercises</p></blockquote><ol><li>Create a new file</li><li>Note the timestamp</li><li>Touch the file and check the timestamp once again</li></ol><hr><h2 id="_6-file" tabindex="-1"><a class="header-anchor" href="#_6-file" aria-hidden="true">#</a> 6. <code>file</code></h2>`,27),A=e("code",null,"banana.jpeg",-1),O=e("code",null,"funny.gif",-1),G=i(`<p>To find out what kind of file a file is, you can use the <code>file</code> command. It will show you a description of the file’s contents.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">file</span> banana.jpg
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p>Run the file command on a few different directories and files and note the output.</p></blockquote><hr><h2 id="_7-cat" tabindex="-1"><a class="header-anchor" href="#_7-cat" aria-hidden="true">#</a> 7. <code>cat</code></h2><p>We’re almost done navigating files, but first let’s learn how to read a file. A simple command to use is the <code>cat</code> command, short for concatenate, it not only displays file contents but it can combine multiple files and show you the output of them.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> dogfile birdfile
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>It’s not great for viewing large files and it’s only meant for <strong>short content</strong>. There are many other tools that we use to view larger text files that we’ll discuss in the next lesson.</p><blockquote><p>Run cat on different files and directories. Then try to cat multiple files.</p></blockquote><hr><h2 id="_8-less" tabindex="-1"><a class="header-anchor" href="#_8-less" aria-hidden="true">#</a> 8. <code>less</code></h2><p>If you are viewing text files larger than a simple output, <code>less</code> is more. (There is actually a command called more that does something similar, so this is ironic.) The text is displayed in a paged manner, so you can navigate through a text file page by page.</p><p>Go ahead and look at the contents of a file with <code>less</code>. Once you’re in the <code>less</code> command, you can actually use other keyboard commands to navigate in the file.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">less</span> /home/pete/Documents/text1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Use the following command to navigate through less:</p><ul><li><kbd>q</kbd> - Used to quit out of less and go back to your shell.</li><li>Page up, Page down, Up and Down - Navigate using the arrow keys and page keys.</li><li><kbd>g</kbd> - Moves to beginning of the text file.</li><li><kbd>G</kbd> - Moves to the end of the text file.</li><li><code>/search</code> - You can search for specific text inside the text document. Prefacing the words you want to search with <code>/</code></li><li><kbd>h</kbd> - If you need a little help about how to use less while you’re in less, use help.</li></ul><blockquote><p>Run <code>less</code> on a file, then page up and around the file. Try searching for a specific word. Quickly navigate to the beginning or the end of the file.</p></blockquote><hr><h2 id="_9-history" tabindex="-1"><a class="header-anchor" href="#_9-history" aria-hidden="true">#</a> 9. <code>history</code></h2><p>In your shell, there is a history of the commands that you previously entered, you can actually look through these commands. This is quite useful when you want to find and run a command you used previously without actually typing it again.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">history</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Want to run the same command you did before, just hit the up arrow.</p><p>Want to run the previous command without typing it again? Use <code>!!</code>. If you typed cat file1 and want to run it again, you can actually just go <code>!!</code> and it will run the last command you ran.</p><p>Another history shortcut is <kbd>ctrl</kbd>-<kbd>R</kbd>, this is the reverse search command, if you hit <kbd>ctrl</kbd>-<kbd>R</kbd> and you start typing parts of the command you want it will show you matches and you can just navigate through them by hitting the <kbd>ctrl</kbd>-<kbd>R</kbd> key again. Once you found the command you want to use again, just hit the <kbd>Enter</kbd> key.</p><p>Our terminal is getting a little cluttered no? Let’s do a little cleanup, use the clear command to clear up your display.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">clear</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>There that looks better doesn’t it?</p><p>While we are talking about useful things, one of the most useful features in any command-line environment is tab completion. If you start typing the beginning of a command, file, directory, etc and hit the Tab key, it will autocomplete based on what it finds in the directory you are searching as long as you don’t have any other files that start with those letters. For example if you were trying to run the command chrome, you can type chr and press Tab and it will autocomplete chrome.</p><blockquote><p>Navigate through your previous command history with the Up and Down keys. Play around with <kbd>ctrl</kbd>-<kbd>R</kbd> reverse search.</p></blockquote><hr><h2 id="_10-cp-copy" tabindex="-1"><a class="header-anchor" href="#_10-cp-copy" aria-hidden="true">#</a> 10. <code>cp</code> (Copy)</h2><p>Let’s start making some copies of these files. Much like copy and pasting files in other operating systems, the shell gives us an even simpler way of doing that.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cp</span> mycoolfile /home/pete/Documents/cooldocs
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,33),S=e("code",null,"mycoolfile",-1),E=e("code",null,"/home/pete/Documents/cooldocs",-1),U=i(`<p>You can copy multiple files and directories as well as use wildcards. A wildcard is a character that can be substituted for a pattern based selection, giving you more flexibility with searches. You can use wildcards in every command for more flexibility.</p><ul><li><code>*</code> the wildcard of wildcards, it&#39;s used to represent all single characters or any string.</li><li><code>?</code> used to represent one character</li><li><code>[]</code> used to represent any character within the brackets</li></ul><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cp</span> *.jpg /home/pete/Pictures
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This will copy all files with the <code>.jpg</code> extension in your current directory to the Pictures directory.</p><p>A useful command is to use the <code>-r</code> flag, this will <strong>recursively</strong> copy the files and directories within a directory.</p><p>Try to do a <code>cp</code> on a directory that contains a couple of files to your Documents directory. Didn’t work did it? Well that’s because you’ll need to copy over the files and directories inside as well with <code>-r</code> command.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cp</span> <span class="token parameter variable">-r</span> Pumpkin/ /home/pete/Documents
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>One thing to note, if you copy a file over to a directory that has the same filename, the file will be overwritten with whatever you are copying over. This is no bueno if you have a file that you don’t want to get accidentally overwritten. You can use the <code>-i</code> flag (interactive) to prompt you before overwriting a file.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cp</span> <span class="token parameter variable">-i</span> mycoolfile /home/pete/Pictures
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p>Copy over a couple of files, be careful not to overwrite anything important.</p></blockquote><hr><h2 id="_11-mv-move" tabindex="-1"><a class="header-anchor" href="#_11-mv-move" aria-hidden="true">#</a> 11. <code>mv</code> (Move)</h2><p>Used for moving files and also renaming them. Quite similar to the <code>cp</code> command in terms of flags and functionality.</p><p>You can rename files like this:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mv</span> oldfile newfile
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Or you can actually move a file to a different directory:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mv</span> file2 /home/pete/Documents
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>And move more than one file:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mv</span> file_1 file_2 /somedirectory
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can rename directories as well:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mv</span> directory1 directory2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Like <code>cp</code>, if you mv a file or directory it will overwrite anything in the same directory. So you can use the <code>-i</code> flag to prompt you before overwriting anything.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mv</span> <span class="token parameter variable">-i</span> directory1 directory2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Let’s say you did want to mv a file to overwrite the previous one. You can also make a backup of that file and it will just rename the old version with a <code>~</code>.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mv</span> <span class="token parameter variable">-b</span> directory1 directory2
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p>Rename a file, then move that file to a different directory.</p></blockquote><hr><h2 id="_12-mkdir-make-directory" tabindex="-1"><a class="header-anchor" href="#_12-mkdir-make-directory" aria-hidden="true">#</a> 12. <code>mkdir</code> (Make Directory)</h2><p>We’re gonna need some directories to store all these files we’ve been working on. The <code>mkdir</code> command (Make Directory) is useful for that, it will create a directory if it doesn’t already exist. You can even make multiple directories at the same time.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mkdir</span> books paintings
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can also create subdirectories at the same time with the <code>-p</code> (parent flag).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">mkdir</span> <span class="token parameter variable">-p</span> books/hemmingway/favorites
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p>Make a couple of directories and move some files into that directory.</p></blockquote><hr><h2 id="_13-rm-remove" tabindex="-1"><a class="header-anchor" href="#_13-rm-remove" aria-hidden="true">#</a> 13. <code>rm</code> (Remove)</h2><p>Now I think we have too many files, let’s remove some files. To remove files you can use the <code>rm</code> command. The <code>rm</code> (remove) command is used to delete files and directories.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">rm</span> file1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Take caution when using <code>rm</code>, there is no magical trash can that you can fish out removed files. Once they are gone, they are gone for good, so be careful.</p><p>Fortunately there are some safety measures put into place, so the average joe can’t just remove a bunch of important files. Write-protected files will prompt you for confirmation before deleting them. If a directory is write-protected it will also not be easily removed.</p><p>Now if you don’t care about any of that, you can absolutely remove a bunch of files.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">rm</span> <span class="token parameter variable">-f</span> file1
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><code>-f</code> or force option tells <code>rm</code> to remove all files, whether they are write protected or not, without prompting the user (as long as you have the appropriate permissions).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">rm</span> <span class="token parameter variable">-i</span> <span class="token function">file</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Adding the <code>-i</code> flag like many of the other commands, will give you a prompt on whether you want to actually remove the files or directories.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">rm</span> <span class="token parameter variable">-r</span> directory
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can’t just rm a directory by default, you’ll need to add the <code>-r</code> flag (<strong>recursive</strong>) to remove all the files and any subdirectories it may have.</p><p>You can remove a directory with the <code>rmdir</code> command.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">rmdir</span> directory
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol><li>Create a file called <code>-file</code> (don&#39;t forget the dash!).</li><li>Remove that file.</li></ol><hr><h2 id="_14-find" tabindex="-1"><a class="header-anchor" href="#_14-find" aria-hidden="true">#</a> 14. <code>find</code></h2><p>With all these files we have on the system it can get a little hectic trying to find a specific one. Well there’s a command we can use for that, <code>find</code>!</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">find</span> /home <span class="token parameter variable">-name</span> puppies.jpg
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,53),V=e("code",null,"puppies.jpg",-1),B=i(`<p>You can specify what type of file you are trying to find.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">find</span> /home <span class="token parameter variable">-type</span> d <span class="token parameter variable">-name</span> MyFolder
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can see that I set the type of file I’m trying to find as (<code>d</code>) for directory and I’m still searching by the name of <code>MyFolder</code>.</p><p>One cool thing to note is that <code>find</code> doesn’t stop at the directory you are searching, it will look inside any subdirectories that directory may have as well.</p><blockquote><p>Find a file from the root directory that has the word <code>net</code> in it.</p></blockquote><hr><h2 id="_15-help" tabindex="-1"><a class="header-anchor" href="#_15-help" aria-hidden="true">#</a> 15. <code>help</code></h2><p>Linux has some great built-in tools to help you how to use a command or check what flags are available for a command. One tool, <code>help</code>, is a built-in bash command that provides help for other bash commands (<code>echo</code>, <code>logout</code>, <code>pwd</code>, <code>etc</code>).</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">help</span> <span class="token builtin class-name">echo</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This will give you a description and the options you can use when you want to run <code>echo</code>. For other executable programs, it’s convention to have an option called <code>--help</code> or something similar.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> <span class="token parameter variable">--help</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Not all developers who ship out executables will conform to this standard, but it’s probably your best shot to find some help on a program.</p><blockquote><p>Run <code>help</code> on the <code>echo</code> command, <code>logout</code> command and <code>pwd</code> command.</p></blockquote><hr><h2 id="_16-man" tabindex="-1"><a class="header-anchor" href="#_16-man" aria-hidden="true">#</a> 16. <code>man</code></h2><p>Gee I wish some of these programs had a manual so we can see some more information about them. Well luckily they do! Aptly named <code>man</code> pages, you can see the manuals for a command with the <code>man</code> command.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">man</span> <span class="token function">ls</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Man pages are manuals that are by default built into most Linux operating systems. They provide documentation about commands and other aspects of the system.</p><p>Try it out on a few commands to get more information about them.</p><blockquote><p>Run the <code>man</code> command on the <code>ls</code> command.</p></blockquote><hr><h2 id="_17-whatis" tabindex="-1"><a class="header-anchor" href="#_17-whatis" aria-hidden="true">#</a> 17. <code>whatis</code></h2><p>Whew, we’ve learned quite a bit of commands so far, if you are ever feeling doubtful about what a command does, you can use the <code>whatis</code> command. The <code>whatis</code> command provides a brief description of command line programs.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>whatis <span class="token function">cat</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The description gets sourced from the manual page of each command. If you ran whatis cat, you’d see there is a small blurb with a short description.</p><blockquote><p>Run the <code>whatis</code> command on the <code>less</code> command.</p></blockquote><hr><h2 id="_18-alias" tabindex="-1"><a class="header-anchor" href="#_18-alias" aria-hidden="true">#</a> 18. <code>alias</code></h2><p>Sometimes typing commands can get really repetitive, or if you need to type a long command many times, it’s best to have an alias you can use for that. To create an alias for a command you simply specify an alias name and set it to the command.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">alias</span> <span class="token assign-left variable">foobar</span><span class="token operator">=</span><span class="token string">&#39;ls -la&#39;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now instead of typing <code>ls -la</code>, you can type foobar and it will execute that command, pretty neat stuff. Keep in mind that this command won&#39;t save your alias after reboot, so you&#39;ll need to add a permanent alias in:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>~/.bashrc
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>or similar files if you want to have it persist after reboot.</p><p>You can remove aliases with the <code>unalias</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">unalias</span> foobar
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p>Create a couple of aliases then remove them.</p></blockquote><hr><h2 id="_19-exit" tabindex="-1"><a class="header-anchor" href="#_19-exit" aria-hidden="true">#</a> 19. <code>exit</code></h2><p>Well, you sure did a good job getting through the basics. We’ve only scratched the surface, now that you’ve learned to crawl, in the next set of courses, I’m gonna teach how to walk.</p><p>For now, you can pat yourself on the back and take a break. To exit from the shell, you can use the <code>exit</code> command</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">exit</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Or the <code>logout</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">logout</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Or if you are working out of a terminal GUI, you can just close the terminal, see you in the next course!</p><blockquote><p>Exit out of the shell and see what happens. Make sure you don&#39;t need to do anymore work in that shell.</p></blockquote>`,45);function $(d,z){const n=l("router-link"),c=l("VPCard"),t=l("FontIcon");return u(),p("div",null,[e("h1",f,[g,a(" "+r(d.$frontmatter.title)+" 관련",1)]),e("blockquote",null,[e("p",null,r(d.$frontmatter.description),1)]),e("nav",y,[e("ul",null,[e("li",null,[o(n,{to:"#_1-the-shell"},{default:s(()=>[a("1. The Shell")]),_:1})]),e("li",null,[o(n,{to:"#_2-pwd-print-working-directory"},{default:s(()=>[a("2. pwd (Print Working Directory)")]),_:1})]),e("li",null,[o(n,{to:"#_3-cd-change-directory"},{default:s(()=>[a("3. cd (Change Directory)")]),_:1})]),e("li",null,[o(n,{to:"#_4-ls-list-directories"},{default:s(()=>[a("4. ls (List Directories)")]),_:1})]),e("li",null,[o(n,{to:"#_5-touch"},{default:s(()=>[a("5. touch")]),_:1})]),e("li",null,[o(n,{to:"#_6-file"},{default:s(()=>[a("6. file")]),_:1})]),e("li",null,[o(n,{to:"#_7-cat"},{default:s(()=>[a("7. cat")]),_:1})]),e("li",null,[o(n,{to:"#_8-less"},{default:s(()=>[a("8. less")]),_:1})]),e("li",null,[o(n,{to:"#_9-history"},{default:s(()=>[a("9. history")]),_:1})]),e("li",null,[o(n,{to:"#_10-cp-copy"},{default:s(()=>[a("10. cp (Copy)")]),_:1})]),e("li",null,[o(n,{to:"#_11-mv-move"},{default:s(()=>[a("11. mv (Move)")]),_:1})]),e("li",null,[o(n,{to:"#_12-mkdir-make-directory"},{default:s(()=>[a("12. mkdir (Make Directory)")]),_:1})]),e("li",null,[o(n,{to:"#_13-rm-remove"},{default:s(()=>[a("13. rm (Remove)")]),_:1})]),e("li",null,[o(n,{to:"#_14-find"},{default:s(()=>[a("14. find")]),_:1})]),e("li",null,[o(n,{to:"#_15-help"},{default:s(()=>[a("15. help")]),_:1})]),e("li",null,[o(n,{to:"#_16-man"},{default:s(()=>[a("16. man")]),_:1})]),e("li",null,[o(n,{to:"#_17-whatis"},{default:s(()=>[a("17. whatis")]),_:1})]),e("li",null,[o(n,{to:"#_18-alias"},{default:s(()=>[a("18. alias")]),_:1})]),e("li",null,[o(n,{to:"#_19-exit"},{default:s(()=>[a("19. exit")]),_:1})])])]),w,o(c,m(v({title:"Command Line",desc:"Learn the fundamentals of the command line, navigating files, directories and more.",link:"https://linuxjourney.com/lesson/the-shell",logo:"https://linuxjourney.com/assets/command-line-dd6a59e10b5ec94f43e805d35d8556dfd070c25871788dd8b5a09a536a477b0a.png",color:"rgba(24, 188, 156, 0.2)"})),null,16),k,e("ul",null,[e("li",null,[a("Absolute path: This is the path from the root directory. The root is the head honcho. The root directory is commonly shown as a slash. Every time your path starts with "),_,a(" it means you are starting from the root directory. For example, "),o(t,{icon:"iconfont icon-folder"}),x,a(".")]),e("li",null,[a("Relative path: This is the path from where you are currently in filesystem. If I was in location "),o(t,{icon:"iconfont icon-folder"}),T,a(" and wanted to get to a directory inside Documents called taxes, I don’t have to specify the whole path from root like "),o(t,{icon:"iconfont icon-folder"}),q,a(", I can just go to "),I,a(" instead. Now that you know how paths work, we just need something to help us change to the directory we want to. Luckily, we have cd or “change directory” to do that.")])]),j,e("p",null,[a("So now I've changed my directory location to "),o(t,{icon:"iconfont icon-folder"}),D,a(".")]),W,e("p",null,[a("Notice how I just used the name of the folder? It’s because I was already in "),o(t,{icon:"iconfont icon-folder"}),N,a(".")]),R,e("ul",null,[L,P,e("li",null,[M,a(" (home directory). This directory defaults to your “home directory”. Such as "),o(t,{icon:"iconfont icon-home"}),C,a(".")]),Y]),F,e("p",null,[a("In the previous lesson we learned about touch, let’s go back to that for a bit. Did you notice that the filename didn’t conform to standard naming like you’ve probably seen with other operating systems like Windows? Normally you would expect a file called "),o(t,{icon:"iconfont icon-flower"}),A,a(" and expect a JPEG picture file.")]),e("p",null,[a("In Linux, filenames aren’t required to represent the contents of the file. You can create a file called "),o(t,{icon:"iconfont icon-flower"}),O,a(" that isn’t actually a GIF.")]),G,e("p",null,[S,a(" is the file you want to copy and "),o(t,{icon:"iconfont icon-folder"}),E,a(" is where you are copying the file to.")]),U,e("p",null,[a("With find you’ll have to specify the directory you’ll be searching it, what you’re searching for, in this case we are trying to find a file by the name of "),o(t,{icon:"iconfont icon-flower"}),V,a(".")]),B])}const J=h(b,[["render",$],["__file","01b-command-line.html.vue"]]);export{J as default};
